# Curator Configuration Example
# Copy this to ~/.config/curator/.env and fill in your values

# Your Firecrawl API key (get one at https://firecrawl.dev)
FIRECRAWL_API_KEY=fc-your-key-here

# Default output directory for curated content
DEFAULT_OUTPUT_DIR=./context

# Enable caching to avoid re-fetching URLs
CACHE_ENABLED=true
